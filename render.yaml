services:
  - type: web
    name: assistente-demandas-frontend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app_streamlit.py --server.port 10000 --server.address 0.0.0.0
    plan: free
